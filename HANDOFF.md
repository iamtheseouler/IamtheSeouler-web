# 원고 넘기기

이 프로젝트에서 하는 일은 **글과 사진 판단까지**다. 발행은 하지 않는다.

파일명 짓기, 사진 리사이즈, 마크다운 앞머리(front matter) 만들기, HTML 만들기,
업로드 — 하나도 하지 않는다. 사이트 구조는 바뀌고, 이 창은 그 변화를 모른다.
발행은 저장소에 손이 닿는 창에서 한다.

---

## 넘기는 것 — 딱 세 가지

### 1. 한국어 원고

소제목은 `### 1. 제목` 형식. 번호와 마침표를 붙인다.

### 2. 영어 원고

같은 구조, 같은 순서. 소제목 번호도 같게.

### 3. 사진 원본

**줄이지 않은 원본 그대로.** 리사이즈·샤프닝·포맷 변환을 하지 않는다.
사이트가 빌드할 때 여러 크기로 알아서 만든다. 미리 줄이면 그 변환이 손해를 본다.

크롭과 톤 보정은 해도 된다. 다만 **크기는 건드리지 않는다.**

사진마다 아래 두 가지를 적어 함께 넘긴다.

- **설명** — 화면에 안 보이지만 화면 낭독기와 검색이 읽는다
- **한 줄** — 갤러리에서 사진 밑에 뜬다. 없으면 비워도 된다

본문에 들어갈 사진은 **영문·한글 원고의 같은 자리에** 표시한다.

```
![설명](사진파일이름 "갤러리 한 줄")
```

본문에 다 못 넣은 사진은 따로 묶어서 "다 싣지 못한 사진"이라고 표시한다.

---

## 함께 적어줄 것

원고 맨 앞에 이만큼만 적는다. 나머지는 발행하는 쪽에서 채운다.

```
제목(영문):
제목(한글):
언제·어디서(영문):    예) Aug 2026 · Jinsang
언제·어디서(한글):    예) 2026년 8월 · 진상
여는 문단(영문):
여는 문단(한글):
홈 카드에 쓸 제목:
홈 카드에 쓸 사진:
```

**"언제·어디서(영문)"의 가운뎃점 뒤가 갤러리 필터 버튼 이름이 된다.**
`Aug 2026 · Jinsang` 이면 버튼이 `JINSANG` 이 된다. 짧은 한 단어로 쓴다.

---

## 하지 않을 것

- 파일 이름 짓기 (`jinsang.md` 같은 것)
- 사진 크기 줄이기
- HTML 만들기
- 언어 전환 버튼 만들기 — 사이트에 이미 있다
- 마크다운 앞머리 작성
- 업로드

이것들을 하면 발행하는 쪽에서 되돌려야 한다. 오늘까지 그랬다.
